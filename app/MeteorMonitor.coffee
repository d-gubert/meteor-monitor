class MeteorMonitor

# findCommand = 'find /home/ -name ".meteor" | grep -E "\\.meteor"'

# Shell findCommand, (error, stdout, stderr) ->
# 	output = ''
# 	for path in stdout.split("\n") when path and not FileSystem.lstatSync(path+'/packages').isDirectory()
# 		output += path+"<br/> "
